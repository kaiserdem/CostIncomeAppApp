
import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Налаштування")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}
